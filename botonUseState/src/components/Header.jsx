import styled from "@emotion/styled";

const header = styled.h1`
    color: red;
    display: flex;
    justify-content: center;
`

export function Header() {
  return (
    <header>Mi formulario</header>
  )
}

export default Header